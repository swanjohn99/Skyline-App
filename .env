# Frontend (Vite) env. Optional in dev.
# Where the SPA reaches the PHP API. Defaults to same-origin '/api'.
# VITE_API_BASE="/api"
# Dev-only: target the Vite proxy forwards /api to (your local PHP server).
VITE_DEV_API_TARGET="http://localhost:8000"
